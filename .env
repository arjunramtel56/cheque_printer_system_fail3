# Reactify Cheque Printer System — Environment Variables
#
# The current build is client-side only and reads none of these variables.
# They are required for the V1.8 server-side hardening phase.
#
# Uncomment and set when Prisma/server-side auth is added.

# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE"
