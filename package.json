{
  "name": "reactify-cheque-system",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "clean": "turbo clean",
    "db:generate": "pnpm --filter @reactify/database generate",
    "db:migrate": "pnpm --filter @reactify/database migrate",
    "db:seed": "pnpm --filter @reactify/database seed"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  }
}
