# Reactify Cheque Printer System — Environment Variables
#
# The current build is client-side only and reads none of these variables.
# They are required for the V1.8 server-side hardening phase (API routes,
# Prisma persistence, server-side auth).
#
# Copy this file to .env.local before running the server or migrations.

# PostgreSQL connection string for the Prisma database.
# Required by: api routes, prisma/seed.ts, admin persistence
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE"

# Next.js environment (set automatically; kept here for documentation)
# NEXTAUTH_URL=http://localhost:3000
